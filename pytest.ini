[pytest]
testpaths = tests/integration tests/e2e
markers =
    integration: Integration tests
    e2e: End-to-end browser tests
asyncio_mode = auto
addopts = -v --tb=short

[pytest-xdist]
workers = auto
dist = loadfile
